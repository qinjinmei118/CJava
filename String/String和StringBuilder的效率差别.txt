String:
在进行字符串连接时，开辟空间，通过copy将字符串拼接到申请的内存空间，
每次拼接都要重新申请新的内存；
StringBuilder：
在进入字符串连接时，类似链表，一开始申请很大的空间，然后直接添加进去。
StringBuilder对象的append效率高于两个String对象的“+”连接操作。
非多线程操作字符串缓存区建议使用StringBuilder;
在编译阶段能够知道字符串常量，完全没必要创建String或StringBuffer对象，
就直接使用字符串常量“+”连接操作效率更高。
